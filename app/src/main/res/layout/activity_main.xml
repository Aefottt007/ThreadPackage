<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="20dp">

        <ProgressBar
            android:id="@+id/pb_download_one"
            android:layout_width="240dp"
            android:layout_height="30dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:background="@null"
            android:progressDrawable="@drawable/layer_list_progressbar"/>

        <Button
            android:id="@+id/btn_download_one"
            android:layout_width="80dp"
            android:layout_height="40dp"
            android:text="下载"
            android:padding="0dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_btn"
            app:backgroundTint="@null"
            tools:ignore="HardcodedText" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="20dp">

        <ProgressBar
            android:id="@+id/pb_download_two"
            android:layout_width="240dp"
            android:layout_height="30dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:background="@null"
            android:progressDrawable="@drawable/layer_list_progressbar"/>

        <Button
            android:id="@+id/btn_download_two"
            android:layout_width="80dp"
            android:layout_height="40dp"
            android:text="下载"
            android:padding="0dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_btn"
            app:backgroundTint="@null"
            tools:ignore="HardcodedText" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="20dp">

        <ProgressBar
            android:id="@+id/pb_download_three"
            android:layout_width="240dp"
            android:layout_height="30dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:background="@null"
            android:progressDrawable="@drawable/layer_list_progressbar"/>

        <Button
            android:id="@+id/btn_download_three"
            android:layout_width="80dp"
            android:layout_height="40dp"
            android:text="下载"
            android:padding="0dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_btn"
            app:backgroundTint="@null"
            tools:ignore="HardcodedText" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="20dp">

        <ProgressBar
            android:id="@+id/pb_download_four"
            android:layout_width="240dp"
            android:layout_height="30dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:background="@null"
            android:progressDrawable="@drawable/layer_list_progressbar"/>

        <Button
            android:id="@+id/btn_download_four"
            android:layout_width="80dp"
            android:layout_height="40dp"
            android:text="下载"
            android:padding="0dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_btn"
            app:backgroundTint="@null"
            tools:ignore="HardcodedText" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginTop="20dp">

        <ProgressBar
            android:id="@+id/pb_download_five"
            android:layout_width="240dp"
            android:layout_height="30dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:background="@null"
            android:progressDrawable="@drawable/layer_list_progressbar"/>

        <Button
            android:id="@+id/btn_download_five"
            android:layout_width="80dp"
            android:layout_height="40dp"
            android:text="下载"
            android:padding="0dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/bg_btn"
            app:backgroundTint="@null"
            tools:ignore="HardcodedText" />

    </LinearLayout>

</LinearLayout>